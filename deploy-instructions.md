# 📦 Instruksi Deploy PADRP ASSYUKRO ke Hostinger\n\n## Status: ✅ Siap Deploy!\n\n### Detail Server\n- **Domain**: https://lime-oyster-135629.hostingersite.com\n- **Database**: `kauman`\n- **DB User**: `u364505515_kauman`\n- **DB Pass**: `Khoirulumam01`\n\n## 🚀 Langkah Deploy Manual (cPanel)\n\n### 1. ZIP Project (lokal)\n```\npowerhell Compress-Archive -Path * -DestinationPath assyukro.zip -Exclude .git,node_modules/,storage/logs/*\n```\n\n### 2. Upload & Extract\n1. Login cPanel Hostinger\n2. File Manager → public_html\n3. Upload `assyukro.zip` → Extract\n4. **Pindahkan SEMUA isi folder `public/` ke `public_html/`**\n5. Upload `.env.production` (rename jadi `.env`) ke root\n6. Hapus ZIP & folder lama\n\n### 3. Setup Database\n1. cPanel → phpMyAdmin\n2. Login dengan user `u364505515_kauman` / `Khoirulumam01`\n3. Pastikan DB `kauman` kosong\n4. **Import `assyukro.sql` (jika punya)** atau jalankan migrations via SSH\n\n### 4. Update .env di Server\nEdit `.env`:\n```\nAPP_URL=https://lime-oyster-135629.hostingersite.com\nDB_HOST=localhost  # atau IP dari cPanel\nDB_DATABASE=kauman\nDB_USERNAME=u364505515_kauman\nDB_PASSWORD=Khoirulumam01\nAPP_ENV=production\nAPP_DEBUG=false\n```\n\n### 5. Jalankan Commands (jika SSH tersedia)\n```\ncd public_html\ncomposer install --no-dev --optimize-autoloader\nphp artisan key:generate\nphp artisan migrate:fresh --seed\nphp artisan storage:link\nphp artisan config:cache\nphp artisan route:cache\nphp artisan view:cache\n```\n\n### 6. Permissions\nstorage/ & bootstrap/cache/ → 755\n\n### 7. Test\n- Buka https://lime-oyster-135629.hostingersite.com\n- Login Admin: admin@kauman.id / password123\n\n## ⚠️ Troubleshooting\n- **500 Error**: Cek .env DB creds, permissions\n- **Assets hilang**: npm run build ulang\n- **DB Error**: Cek host (localhost atau dari cPanel)\n\n**✅ Project siap di folder saat ini. ZIP & upload sekarang!**\n
